## cinc org invite list

List pending invitations for the current org

```
cinc org invite list [flags]
```

### Examples

List the pending invitations for the org your profile points at.

```bash
cinc org invite list
```

### Options

```
  -h, --help   help for list
```

### Options inherited from parent commands

```
      --config string    path to the Cinc credentials file (default ~/.cinc/credentials)
      --format string    output format: human or json (default "human")
      --profile string   credentials profile to use (default: $CINC_PROFILE, then $CHEF_PROFILE, then "default")
```

### SEE ALSO

* [cinc org invite](cinc_org_invite.md)	 - List, create, or rescind invitations for the current org

